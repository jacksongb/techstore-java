spring:
  application:
    name: techstore
  cloud:
    nacos:
      config:
        server-addr: ${NACOS_ADDR:119.29.145.64:8848}
        namespace: ${NACOS_NAMESPACE:}
        group: DEFAULT_GROUP
        file-extension: yml
